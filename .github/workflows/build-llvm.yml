name: Build LLVM (reusable)
on:
  workflow_call:
    inputs:
      platform:
        description: "bionic | linux | bsd | windows | macos"
        required: true
        type: string
      targets:
        description: "JSON array of target triples"
        required: true
        type: string
      patchset:
        description: "extra patch dir under patches/ (e.g. musl)"
        required: false
        type: string
        default: ""
      version:
        required: true
        type: string
      revision:
        required: false
        type: string
        default: ""
      projects:
        required: true
        type: string
      pgo:
        description: "use the published PGO profile for this llvm revision"
        required: false
        type: boolean
        default: true
      mlgo:
        description: "embed the arm64 MLGO models where the target supports it"
        required: false
        type: boolean
        default: true
      lto:
        description: "ThinLTO; applied everywhere except macos"
        required: false
        type: boolean
        default: true

permissions:
  contents: write
  packages: read

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(inputs.targets) }}
    runs-on: ubuntu-24.04
    steps:
      - name: Maximize build space
        uses: easimon/maximize-build-space@v10
        with:
          root-reserve-mb: 5120
          swap-size-mb: 16384
          remove-dotnet: true
          remove-android: true
          remove-haskell: true
          remove-codeql: true
          remove-docker-images: true
          build-mount-path: /mnt

      - name: Move Docker to /mnt
        run: |
          sudo systemctl stop docker.socket docker.service
          sudo mkdir -p /mnt/docker
          sudo rsync -aqP /var/lib/docker/ /mnt/docker/
          sudo rm -rf /var/lib/docker
          sudo ln -s /mnt/docker /var/lib/docker
          sudo systemctl start docker

      - name: Prepare /mnt/build
        run: |
          sudo mkdir -p /mnt/build
          sudo chown $(id -u):$(id -g) /mnt/build
          rmdir "$GITHUB_WORKSPACE"
          ln -s /mnt/build "$GITHUB_WORKSPACE"

      - uses: actions/checkout@v5

      - name: Log in to GHCR
        uses: docker/login-action@v4.2.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build LLVM
        run: |
          IMAGE="ghcr.io/$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]')/llvm-builder:latest"
          docker run --rm \
            -v "/mnt/build:/work" -w /work --user "$(id -u):$(id -g)" \
            -e HOME=/work \
            -e PLATFORM='${{ inputs.platform }}' \
            -e TARGET='${{ matrix.target }}' \
            -e PATCHSET='${{ inputs.patchset }}' \
            -e NDK_VERSION='${{ inputs.version }}' \
            -e NDK_REVISION='${{ inputs.revision }}' \
            -e PROJECTS='${{ inputs.projects }}' \
            -e ROOTDIR=/work \
            -e LLVM_BUILD_ID='${{ github.run_id }}' \
            -e ENABLE_PGO='${{ inputs.pgo && 1 || 0 }}' \
            -e ENABLE_MLGO='${{ inputs.mlgo && 1 || 0 }}' \
            -e LLVM_LTO='${{ inputs.lto && 'Thin' || 'OFF' }}' \
            -e PGO_URL_BASE='https://github.com/${{ github.repository }}/releases/download/profdata' \
            -e CLANG_REPOSITORY_STRING='https://github.com/${{ github.repository }} ${{ github.sha }}' \
            -e PACKAGE_BUGREPORT='https://github.com/${{ github.repository_owner }}/android-ndk-custom/issues/' \
            "$IMAGE" \
            bash -c 'scripts/fetch-source.sh && scripts/build.sh'

      - name: Package
        run: |
          cd /mnt/build
          projects=$(echo '${{ inputs.projects }}' | tr ";" "+")
          tarball="${projects}-r${{ inputs.version }}${{ inputs.revision }}-${{ matrix.target }}"
          mv "llvm-${{ matrix.target }}" "$tarball"
          tar -c "$tarball" | xz -T0 -v > "/mnt/$tarball.tar.xz"
          echo "tarball=$tarball" >> "$GITHUB_ENV"

      - name: Upload release
        uses: ncipollo/release-action@v1.21.0
        with:
          tag: "llvm-r${{ inputs.version }}"
          artifacts: /mnt/${{ env.tarball }}.tar.xz
          allowUpdates: true
          replacesArtifacts: true
          body: ''
